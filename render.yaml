services:
  - type: web
    name: document-organizer-mcp
    runtime: docker
    plan: starter
    autoDeploy: false
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8000
      - key: DOC_ROOT
        value: /app/data
